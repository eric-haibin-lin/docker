### Docker for BERT training
- `make image` to build a container image based on `Dockerfile`
- `make push-image` to build and publish the docker container to remote registry.

Author: Joe Evans, Haibin Lin
